{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build:web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "if git rev-parse --verify HEAD^ >/dev/null 2>&1; then git diff HEAD^ HEAD --quiet -- apps/web packages/database && exit 0 || exit 1; else exit 1; fi"
}

