{
  "name": "plataforma-dental",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "dev:web": "turbo dev --filter=web",
    "dev:whatsapp": "turbo dev --filter=whatsapp-service",
    "build:web": "turbo build --filter=web",
    "build:whatsapp": "turbo build --filter=whatsapp-service"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.0.0"
}
